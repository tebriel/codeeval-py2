#! /usr/bin/python

import sys

if __name__ == "__main__":
    if len(sys.argv) > 1:
        lines = None
        with open(sys.argv[1], 'r') as f:
            total = 0
            for line in f.readlines():
                total += int(line)

            print total
